import { PrismaExceptionHandlerFilter } from './prisma-exception-handler.filter';

describe('PrismaExceptionHandlerFilter', () => {
  it('should be defined', () => {
    expect(new PrismaExceptionHandlerFilter()).toBeDefined();
  });
});
